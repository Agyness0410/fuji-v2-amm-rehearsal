# 课程任务：让 AI 帮你改合约，但由你决定规则

## 最终交付

你需要交付一个公开 GitHub 仓库、一个通过 CI 的 commit、两个由你部署在 Fuji 的合约地址，以及一个能连接 Fuji 的 Vercel 页面。

AI 可以写代码、跑测试、执行你授权的部署和推送命令；规则选择、部署密码输入、密钥保管和最终公开确认属于学员。Hardhat 只在本机内存中签名，AI 不接触秘密。

## 基线观察

1. 在本地 Claim 1,000 COURSE。
2. Approve 后用 AVAX 和 COURSE 建立首池。
3. 记录储备 `x`、`y` 和 `k`。
4. 做一次 AVAX → COURSE swap，再记录三者。
5. 解释为什么成交价不同于页面执行前的边际价格，以及 0.30% fee 留在了哪里。

## 必做合约改动（任选一项）

- 把 fee 从 0.30% 改成另一个明确数值，并说明谁受益、谁承担成本。
- 改变 Claim 数量或规则，并讨论女巫攻击为什么让“每地址一次”不等于“每人一次”。
- 增加单笔 swap 上限，写清它缓解了什么风险、又限制了谁。

验收条件：

- 先新增至少一个会失败的测试，再改合约让它通过。
- 保留零输入、deadline、最小输出/最小份额、重入保护和 CEI。
- 前端文案与数值必须同步；不能只改页面而不改合约。
- `yarn ci:contracts` 与前端检查均通过。

## 加分任务

- 在页面展示价格影响，而不依赖中心化 API。
- 对比 `amountOutMin = 0` 与合理滑点保护在抢跑/价格变化下的区别。
- 给 quote 写基于不变量的 property/fuzz test。
- 设计更公平的教学币领取方式，但不要把中心化身份数据直接写上链。

## 不在本课程范围

生产级审计、多池路由、任意 ERC20、预言机、MEV 防护、治理、可升级代理、收益挖矿和真实资产。它们不是“再让 AI 多写一点”就能安全获得的功能。

## 交付检查

- [ ] 我能指出自己改过的 Solidity 行，并解释为什么。
- [ ] 正常路径和无效输入测试都通过。
- [ ] Fuji 部署交易和两个合约 bytecode 已核验。
- [ ] `deployedContracts.ts` 是部署命令自动生成的。
- [ ] 仓库没有 `.env`、私钥、助记词或密码。
- [ ] GitHub Actions 的 test/lint/typecheck/build/secret scan 通过。
- [ ] Vercel 页面连接的是 chain ID 43113。
- [ ] 页面明确写着“教学用途，不使用真实资产”。
